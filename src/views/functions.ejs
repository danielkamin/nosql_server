<%
getCurrentIdFromCookie=function(documentCookie){
    let id =''
    console.log(documentCookie)
    // documentCookie.split(';').forEach(cookie=>{
    //         let parsed = cookie.split('=')
    //         if(parsed[0] === '_id'){
    //             id=parsed[1]
    //         }
    // })
    return id
}
%> 